{
    "watchers": [
        {
            "name": "production app",
            "doppler_token": "my-secret-doppler-token",
            "docker_services": ["backend", "sidekiq"]
        },
        {
            "name": "staging apps",
            "doppler_token": "my-secret-doppler-token",
            "docker_services": ["staging-*", "preview-*"]
        }
    ]
}
